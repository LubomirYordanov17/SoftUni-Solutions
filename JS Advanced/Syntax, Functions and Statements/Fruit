function solve(fruit,weight,food){
let weighting=weight/1000
let result=weighting*food
console.log(`I need $${result.toFixed(2)} to buy ${weighting.toFixed(2)} kilograms ${fruit}.`)
}
solve('apple', 1563, 2.35)